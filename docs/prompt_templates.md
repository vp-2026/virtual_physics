# Prompt templates

The initial anonymized prompt sketch that previously occupied this file has
been superseded by the exact release protocol in [`../prompts/`](../prompts/).

The authoritative executable prompt builders are:

- `scripts/experiment/forked_feedback_runner.py`
- `scripts/experiment/transfer_sidecar_runner.py`

The prompt folder transcribes the fixed text and marks every run-time
substitution explicitly.
